# 路线三：自己的香港服务器（彻底告别平台风控）

> 目的：把游戏大厅 + 账号云存档整体搬到一台自己的服务器上。
> 从此没有"风险提醒"、没有强制下载、没有白名单规则、没有平台限额。
> 本文档是采购 + 迁移的完整清单，随时可执行，预计半天搞定。

## 为什么是香港

- **大陆服务器**：提供网页服务必须 ICP 备案（买域名、拍照、等 2~3 周），否则云厂商会封 80/443 端口。
- **香港服务器**：不需要备案，国内直连延迟约 30~60ms，孩子玩游戏完全无感。
- 国外更远的地方（日本/美西）延迟高一截，没必要。

## 买什么（合计约 ¥300~500/年）

| 项 | 建议 | 大概价格 |
|---|---|---|
| 服务器 | 腾讯云轻量 / 阿里云轻量，**香港地域**，1核1G 起步（俩静态游戏 + 一个小接口，绰绰有余） | 促销时 ¥100~200/年，日常 ¥25~40/月 |
| 域名 | `.top` / `.xyz` 等便宜后缀就行；想要 `.com` 也行 | ¥10~70/年 |
| HTTPS 证书 | Let's Encrypt，Caddy 自动申请自动续期 | 免费 |

域名买完后在注册商那里做一次**实名认证**（传身份证，当天过）——这是注册局要求，不是备案，不耽误使用。

## 架构（刻意保持极简）

```
孩子浏览器 ──HTTPS──> Caddy（唯一对外的程序，自动证书）
                        ├── 静态文件直出：/  /2048/  /count-master/  /test/
                        └── /api/* → 本机 Node 小服务（kids-api 逻辑改写，约 100 行）
                                          └── SQLite 文件 kids.db（accounts / saves 两张表）
```

- **不用装数据库**：PostGIS/PostgreSQL 那套全部丢掉，SQLite 就是一个文件，备份 = 拷走它。
- **kids-api 改写量很小**：login/load/save 三个操作的逻辑（scrypt 校验、HMAC token）原样保留，只是把"调腾讯云 ExecutePGSql"换成"查本地 SQLite"，并套上现有云函数返回包装 `{statusCode, headers, body}`，前端 cloud-account.js 只需要改一个域名常量。
- **静态树直接 rsync**：`site-root/` 当根、`games/*/` 铺子目录，和现在 deploy.sh 拼出来的"线上树"一字不差。

## 迁移步骤（顺序执行，旧站全程不动）

1. 买香港轻量服务器 + 域名，域名 A 记录解析到服务器 IP。
2. 服务器上装 Caddy + Node 20（两条命令），放好 Caddyfile（静态目录 + /api 反代，域名写上即自动 HTTPS）。
3. `rsync` 上传 site-root + games 到服务器 `/srv/games/`。
4. 搬数据：用腾讯云 API `ExecutePGSql` 把 `accounts`/`saves` 两张表 select 出来（数据量极小），灌进 SQLite。
5. 起一个改好的 Node 版 kids-api（监听 127.0.0.1:8787），Caddy 把 `/api/*` 转给它。
6. 改 `cloud-account.js` 的接口常量为 `https://<新域名>/api`，连同样例游戏先部署到**新服务器**上自测：登录、存档、换账号隔离、跨设备同步。
7. 自测通过 → 把新链接通过微信发到学习机玩一天（旧 CloudBase 站点原样保留，随时可切回）。
8. 观察一周稳定 → CloudBase 环境可以停掉省钱；本仓库的 deploy.sh 改成 `rsync` 到服务器（改动约 10 行）。

## 风险和注意

- **微信打开非备案域名**：正规 HTTPS 站一般都直接打开；极少数情况下微信会提示"网页未经备案"，点继续即可。比现在的风控骰子可控得多。
- **服务器要自己备份**：配一个 cron，每天把 kids.db 打包发到这台工作服务器（或对象存储），一行命令的事。
- **学习机自带浏览器**：如果将来用它访问，绿网白名单把新域名加进去即可（自己的域名，加一次永远有效）。
- 服务器安全：只开 22/80/443，SSH 用密钥登录，系统更新交给 unattended-upgrades。

## 回退方案

迁移期间 CloudBase 旧站**一个字节都不动**。新站任何不满意，微信里把旧链接重新发给孩子即完成回滚，零成本。
